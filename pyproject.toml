[project]
name = "hems-echonet"
version = "0.1.0"
description = "Fetch and display Smart Cosmo ECHONET Lite values with pychonet"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "psycopg[binary]>=3.2.0",
  "pychonet>=2.6.18",
]

[tool.uv]
package = false
