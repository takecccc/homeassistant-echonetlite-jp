[project]
name = "hems-echonet"
version = "0.1.0"
description = "Minimal ECHONET Lite client for local HEMS devices"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = false
